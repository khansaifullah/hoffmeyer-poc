export const metadata = {
  title: "Request a Quote | Hoffmeyer",
  description: "Request pricing and availability for Hoffmeyer industrial products.",
};

export default function QuoteLayout({ children }) {
  return children;
}
